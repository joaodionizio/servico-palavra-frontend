# Local: use http://localhost:5000 ou a porta configurada no backend.
NEXT_PUBLIC_API_URL=http://localhost:5000

# Na Vercel, substitua NEXT_PUBLIC_API_URL por /api/backend.

# Obrigatorio somente no servidor (Vercel). Nao use NEXT_PUBLIC_ para a URL real.
API_BACKEND_URL=https://servico-palavra-api.onrender.com
